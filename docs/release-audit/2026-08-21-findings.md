# KanaGame Release Audit Findings — 2026-08-21

| ID | Candidate | Initial severity | Reproduction | Disposition | Evidence / test | Commit |
|---|---|---:|---|---|---|---|
| RA-01 | Review Play Again reuses the previous frozen word pool | Medium | Reproduced: after a six-round Review attempt cleared `a-ai` but kept `a-ie` weak, Play Again still showed six rounds instead of three | Fixed | Root cause: the three word games passed only `rowId` to both frozen word resolution and `useGameSession`, so Retry rebuilt from the completed attempt snapshot. Fix: a page-owned attempt counter forms `${rowId}:${sessionAttempt}`; only Play Again increments it, while Mistake Review keeps the completed snapshot. Covered by hook and Typing/Listening/Word Builder replay regressions. | `fix(review): refresh word pool on replay` |
| RA-02 | Review count excludes independently weak words | Medium | Reproduced: teaching `a-row` and raising only `a-ai`'s word review score to 10 produced `weakWords = ['a-ai']` but `reviewCount = 0`; mixed weak character + weak word state also counted only the character | Fixed | Root cause: `useCurriculum` exposed `reviewCount` as `weakCharacterIds.length`, omitting independently weak words. Fix: expose `reviewCharacterCount` and `reviewWordCount`, sum them for `reviewCount`, and render item-neutral Review copy. Regression coverage asserts word-only (0 + 1 = 1) and mixed (1 + 1 = 2) cases. Focused curriculum/routing tests, full suite, lint, and build pass. | `fix(review): count independently weak words` |
| RA-03 | Queue or finish accounting can report incorrect accuracy | Medium | Not reproduced: focused hook checks keep the in-progress queue stable when live IDs shrink, rebuild explicit replay from the current IDs, report mistake review as `1 / 1`, and report a completed two-round queue once as `1 / 2`. The initial replay assertion expected `['u']`, but the observed `['u', 'u']` is correct for a two-round session with one eligible ID. | Closed | `useGameSession.test.ts` (focused contract coverage); production hook unchanged | `test(session): lock queue and accuracy accounting` |
| RA-04 | Partial/current-version localStorage can break Review state | Medium | Reproduced: a version-5 payload whose character entry omitted `reviewScore` shallow-merged over the live store; subsequent Review-score arithmetic would receive `undefined`. A partial `{ version: 5, state: { characters: {}, taughtRowIds: ['a-row'] } }` envelope also exercised the real rehydration boundary. | Fixed | Root cause: Zustand persist's default merge is shallow, so current-version nested records bypass v4→v5 migration and can replace canonical maps without their newer fields. Fix: version-5 `mergePersistedProgress` validates records, backfills maps/fields, clamps finite review scores, accepts valid persisted settings, and retains current actions/defaults. Pure merge and actual `persist.rehydrate()` regressions pass, along with Review hook tests, full suite, lint, and build. | `fix(progress): normalize persisted review state` |
| RA-05 | Placeholder romaji IDs can create noncanonical accepted answers | Medium | Reproduced: `o-to` was accepted for おっと (`otto`) and `ke-ki` for ケーキ (`keeki`); the same walker also failed to produce the valid `matti` alternate for マッチ (`macchi`) | Fixed | Root cause: `romajiVariants` emitted the catalog placeholder `-` and advanced by its fake one-character width instead of aligning IDs to canonical word substrings. Fix: uniquely align each token to non-empty canonical segments, reject ambiguous alignment, expand alternates only for concrete kana, and keep gemination coupled to an alternate's initial consonant. Regression coverage preserves canonical/multi-token/Kunrei answers while rejecting placeholder and deleted gemination/length. | `fix(answers): preserve contextual romaji markers` |
| RA-06 | Superseded TTS can start Web Speech over current static audio | Medium | Not reproduced: deferred playback rejection from an older request resolves quietly after a newer request supersedes it, while a rejection from the current request still propagates | Already fixed | `StaticFileProvider` already guards both `audioEl.onerror` and `play().catch` with its monotonic request ID; regression coverage added for deferred replacement and current-request failure. Focused static-file/Web Speech provider tests pass (11/11). Existing fix: `31f5878` | `test(audio): cover superseded playback` |
| RA-07 | Repeated Tracing Next skips a round | Medium | Reproduced: with `a-row` taught, two `Next` clicks inside one React `act()` advanced the five-character trace from Round 1 to Round 3. | Fixed | RED: `TracingPage.test.tsx` failed its Round 2 assertion and rendered Round 3. GREEN: an `advanceLockedRef` blocks a second transition until the committed phase/round effect clears it; the same test passes at Round 2. | `fix(games): clean up delayed and repeated advances` |
| RA-08 | Correct-answer timers survive unmount/navigation | Low | Reproduced by inspection: Kana Quiz, Kana Typing, and Listening each scheduled a bare correct-answer `setTimeout(advance, 2000)` with no owner cleanup. | Fixed | RED: `useDelayedAction.test.ts` could not resolve the absent cleanup hook. GREEN: its unmount and replacement timer regressions pass; the three bare answer timers now schedule through the hook, while Word Builder keeps its existing effect cleanup. | `fix(games): clean up delayed and repeated advances` |
| RA-09 | PWA update loses legacy cached audio offline | High | Not reproduced at the Workbox handler level: a `NetworkFirst` request returned bytes preloaded in the legacy `kana-game-media` cache while offline, then returned and cached different network bytes after connectivity was restored. The production preview loaded in the in-app browser, but its controls expose no clean-profile selection, offline/network emulation, Cache Storage mutation, or service-worker inspection, so the exact browser activation/update sequence was not verified. | Already fixed | Existing fix `f9f52d9` restored the legacy cache name and changed audio to `NetworkFirst`. `vite.config.test.ts` now locks that route contract and exercises Workbox's real `NetworkFirst` handler for legacy fallback plus online refresh. The generated `dist/sw.js` registers the audio route before the image route with `cacheName: "kana-game-media"`, `networkTimeoutSeconds: 4`, 500 entries/180 days, and cacheable statuses 0/200; images remain `CacheFirst` with the shared budget. Focused config/handler tests, full suite, lint, and build pass. | `test(pwa): lock legacy audio cache upgrade` |
| RA-10 | Deploy workflow omits lint and tests | Medium | Confirmed by inspection | Fixed | `.github/workflows/deploy.yml` now runs `npm run lint`, `npm test`, then `npm run build` after `npm ci`, with the contract locked by `src/deployWorkflow.test.ts` | `ci: gate deployment on lint and tests` |
| RA-11 | Yōon tracing needs generated multi-glyph stroke paths | Medium | Not reproduced | Intentional constraint | `AGENTS.md` forbids first-codepoint generation for multi-glyph IDs | — |
| RA-12 | Reset action has no production UI entry | Low | Not reproduced | Product decision | No progress-loss or release-blocking path established | — |
