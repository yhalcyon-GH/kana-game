# KanaGame Release Audit Findings — 2026-08-21

| ID | Candidate | Initial severity | Reproduction | Disposition | Evidence / test | Commit |
|---|---|---:|---|---|---|---|
| RA-01 | Review Play Again reuses the previous frozen word pool | Medium | Reproduced: after a six-round Review attempt cleared `a-ai` but kept `a-ie` weak, Play Again still showed six rounds instead of three | Confirmed / Fixed | A page-owned `${rowId}:${sessionAttempt}` key now refreshes the live word pool only for Play Again; Mistake Review keeps the completed snapshot. Hook and Typing/Listening/Word Builder replay regressions pass. | `fad9b25` |
| RA-02 | Review count excludes independently weak words | Medium | Reproduced: word-only weakness produced `weakWords = ['a-ai']` but `reviewCount = 0`; mixed weakness counted only the character | Confirmed / Fixed | `reviewCharacterCount + reviewWordCount` now drives item-neutral Review copy. Curriculum regressions cover word-only (0 + 1 = 1) and mixed (1 + 1 = 2) states. | `ca04afc` |
| RA-03 | Queue or finish accounting can report incorrect accuracy | Medium | Not reproduced: the running queue stays stable as live IDs shrink; explicit replay uses current IDs; mistake review reports `1 / 1`; a completed two-round queue reports `1 / 2` once | Not reproduced / No code change | `useGameSession.test.ts` locks queue freezing, replay, completed-length, and mistake-denominator contracts. The focused final-round double-advance audit also invoked `onFinish` once. | `3a5650e`, `e88bbb1` |
| RA-04 | Partial/current-version localStorage can break Review state | Medium | Reproduced: a version-5 character missing `reviewScore` shallow-merged over the live store, and a partial current-version envelope replaced canonical maps/defaults | Confirmed / Fixed | `mergePersistedProgress` validates records, backfills maps/fields, clamps progress/settings domains, and spreads the current store first so actions/defaults survive. Pure merge and real `persist.rehydrate()` regressions pass. | `b2619b8`, `88ca88a` |
| RA-05 | Placeholder romaji IDs can create noncanonical accepted answers | Medium | Reproduced: `o-to` was accepted for おっと and `ke-ki` for ケーキ, while valid `matti` for マッチ was missing | Confirmed / Fixed | Unique canonical-segment alignment keeps contextual っ/ッ/ー text, expands only concrete-kana alternates, and couples gemination to the selected consonant. Focused regressions pass; a final sweep accepted canonical romaji for all 275 vocabulary entries. | `9fd4c54` |
| RA-06 | Superseded TTS can start Web Speech over current static audio | Medium | Not reproduced: an older deferred playback rejection resolves quietly after supersession, while the current request's rejection still propagates | Already fixed / Regression test added | `StaticFileProvider` guards both `audioEl.onerror` and `play().catch` with its monotonic request ID. Deferred replacement and current-request failure regressions pass. | `31f5878`, `39c90d9` |
| RA-07 | Repeated Tracing Next skips a round | Medium | Reproduced: two Next clicks before rerender advanced the five-character trace from Round 1 to Round 3 | Confirmed / Fixed | RED rendered Round 3; GREEN uses `advanceLockedRef` until the phase/round commit, and the regression remains on Round 2. | `04eea22` |
| RA-08 | Correct-answer timers survive unmount/navigation | Low | Reproduced by inspection: Kana Quiz, Kana Typing, and Listening scheduled bare two-second answer timers with no owner cleanup | Confirmed / Fixed | `useDelayedAction` cancels on unmount and replaces any prior schedule, so only one delayed action is pending; both regressions pass. Word Builder retains its existing effect cleanup. | `04eea22` |
| RA-09 | PWA update loses legacy cached audio offline | High | Not reproduced at the deterministic Workbox boundary: `NetworkFirst` served bytes preloaded in legacy `kana-game-media` while offline, then returned and cached fresh network bytes online. Current browser controls cannot execute a clean-profile activation/update sequence. | Already fixed / Regression test added | Existing fix restores the legacy cache name and uses `NetworkFirst`; the real Workbox-handler regression locks fallback and refresh behavior. Generated `dist/sw.js` registers audio first with a four-second timeout, 500-entry/180-day shared budget, and statuses 0/200; images retain their existing `CacheFirst` rule. | `f9f52d9`, `71bc988` |
| RA-10 | Deploy workflow omits lint and tests | Medium | Confirmed by inspection | Confirmed / Fixed | The build job now runs `npm run lint`, `npm test`, then `npm run build` after `npm ci`; `src/deployWorkflow.test.ts` locks the exact pre-upload gate tail. | `e37eea8`, `0f48123` |
| RA-11 | Yōon tracing needs generated multi-glyph stroke paths | Medium | Not reproduced: yōon deliberately has no combined-glyph KanjiVG entry and safely renders an empty guide | Intentional constraint / No code change | `StrokeOrderAnimation.test.tsx` proves `STROKE_PATHS['kya']` is absent, the empty fallback renders without crashing, and ordinary single-glyph paths still render. First-codepoint generation would mislabel き as きゃ. | — |
| RA-12 | Reset action has no production UI entry | Low | Not reproduced: the action is referenced by store/unit setup only, and no progression failure requires destructive recovery | Intentional constraint / No code change | Adding a progress-erasing UI control is a product decision outside this audit; no release-blocking path or approved reset UX was established. | — |
| RA-13 | Repeated final-round advance may invoke game completion twice | Low | Not reproduced: a one-round hook session given two `advance()` calls in one React `act()` produced `{ completions: 1, finished: true, roundIndex: 0, queueLength: 1 }` | Not reproduced / No code change | `finished` changes from false to true once, and the finish effect—not the state updater—owns `onFinish`; repeated `setFinished(true)` does not retrigger it. | — |
| RA-14 | Malformed legacy persistence payload can throw before defensive merge | Medium | Reproduced only with a syntactically valid legacy envelope whose `state` is `null`: Zustand caught migration failure, retained `{ characters: {}, unlockedRowIds: ['a-row'] }`, and reported `hasHydrated() === false` | Deferred Low | Exact impact: an externally corrupted/non-schema legacy payload cannot restore its progress and remains marked unhydrated, but the app receives a usable default store. Released payloads use object state, localStorage writes are atomic, and Task 11 authorizes production fixes only for newly confirmed Critical/High defects. | — |
| RA-15 | jsdom emits media/canvas not-implemented notices during tests | Low | Reproduced: full and focused suites print `HTMLMediaElement.play()` and `HTMLCanvasElement.getContext()` notices while all assertions pass | Intentional constraint / No code change | These are jsdom API gaps, not browser exceptions. Provider tests mock media playback, tracing regression tests mock canvas context, real browsers supply both APIs, and the final suite has zero failures. | — |

## Final triage summary

- Confirmed Critical: **0**
- Confirmed High: **0**
- Deferred Medium: **0**
- Deferred Low: **1** (`RA-14`, corrupted legacy persistence recovery)
- Every finding has a terminal evidence-backed disposition; no finding remains open.
